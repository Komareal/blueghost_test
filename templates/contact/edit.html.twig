{% extends 'base.html.twig' %}

{% block title %}{{ 'contact.edit.title'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'contact.edit.header'|trans }}</h1>

    {{ include('contact/_form.html.twig', {'button_label': 'contact.edit.update'|trans }) }}

    <a href="{{ path('app_contact_index') }}">{{ 'contact.edit.back_to_list'|trans }}</a>

    {{ include('contact/_delete_form.html.twig') }}
{% endblock %}
